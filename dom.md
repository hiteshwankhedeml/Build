# DOM

* Structured representation of the HTML content, allowing scripts to dynamically access, modify, and manipulate the elements of a webpage

1. Tree Structure:

* The DOM represents the web page as a tree like structure where elements are nodes

2. Hierarchy of nodes
3. Dynamic content manipulator
