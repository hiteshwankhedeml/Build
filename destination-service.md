# Destination Service

* Provides a REST API that you can use to read and manage destinations and certificate on all available levels
* We never have to add sensitive information such as password to our code
* Authentication strategies vary access different destination types and you need to implement each single one
* Dev tools have virtually no use if they cannot access supporting information
