# Scenario - Mass Order Management

* We have a business process to manager single order of a customer and trigger the email&#x20;
* Design a new process where the current process be added inside a loop as a subprocess and call it multiple time for each order of same customer
